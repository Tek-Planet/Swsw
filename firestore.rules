
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    //================================================================================
    // GENERAL-PURPOSE RULES
    //================================================================================

    // Deny all reads and writes by default.
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper function to check if a user is authenticated.
    function isSignedIn() {
      return request.auth != null;
    }

    // Helper function to check if the user is an admin.
    function isAdmin() {
      return request.auth.token.admin == true;
    }

    // Helper function to check ownership of a document.
    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    //================================================================================
    // USER PROFILES
    //================================================================================

    // Users can read anyone's profile.
    // Users can only create and update their own profile.
    match /users/{userId} {
      allow read: if isSignedIn();
      allow create, update: if isOwner(userId);
    }

    //================================================================================
    // EVENTS & SUBCOLLECTIONS
    //================================================================================

    // Any signed-in user can read published events.
    // Admins can create, update, and delete any event.
    match /events/{eventId} {
      allow read: if isSignedIn() && resource.data.status == 'published';
      allow create, update, delete: if isAdmin();
      
      // [NEW RULE] Allow any signed-in user to read survey questions for an event.
      match /survey/{surveyId} {
        allow read: if isSignedIn();
      }

      // Ticket tiers can be read by any signed-in user.
      // Only admins can modify ticket tiers.
      match /ticketTiers/{tierId} {
        allow read: if isSignedIn();
        allow create, update, delete: if isAdmin();
      }
    }

    //================================================================================
    // ORDERS & PAYMENTS
    //================================================================================

    // Users can read and update their own orders.
    // Admins can read any order.
    match /orders/{orderId} {
      allow read, update: if isSignedIn() && request.auth.uid == resource.data.userId;
      allow read: if isAdmin();
    }
    
    //================================================================================
    // PROMO CODES
    //================================================================================
    
    // Promo codes are read-only for clients, modifiable by admins.
    match /promoCodes/{codeId} {
        allow read: if isSignedIn();
        allow write: if isAdmin();
    }
    
    //================================================================================
    // EVENT GRIDS (SURVEY MATCHES)
    //================================================================================
    
    // Users can only read their own result grid.
    // The Cloud Function (running as admin) is the only thing that can write to this.
    match /event_grids/{eventId}/grids/{userId} {
      allow read: if isOwner(userId);
      allow write: if false; // Protect from client-side writes
    }
    
    //================================================================================
    // EVENT SURVEY SUBMISSIONS
    //================================================================================
    
    // Users can create their own submission.
    // Only the Cloud Function (admin) should read these.
    match /event_surveys/{eventId}/submissions/{userId} {
        allow create: if isOwner(userId);
        allow read, write: if false; // Protect from client-side access
    }
  }
}
